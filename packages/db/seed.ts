// This is a placeholder file for database seeding.
// In a real scenario, you would use Prisma Client to create data.

async function main() {
  console.log('Starting database seed...');
  // Example: await prisma.post.create({ data: { title: 'First Post' } });
  console.log('Database seed complete.');
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
